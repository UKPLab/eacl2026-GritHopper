// Base path for GitHub Pages deployment
// Change this if the repository name changes
export const BASE_PATH = "/eacl2026-GritHopper";

// Helper to get asset paths
export const getAssetPath = (path: string) => `${BASE_PATH}${path}`;
